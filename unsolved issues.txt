## 해결된 이슈
✅ 해결됨 (2026-07-12 15:00): **P0 — AC 조류분석 기반 N-1 취약점 분석 + 호몰로지 비교 구현 완료**
   - `power_grid/ac_power_flow.py`: Newton-Raphson AC 조류분석 솔버 (순수 numpy)
   - `power_grid/contingency.py`: N1ContingencyAnalyzer 클래스
     - 각 라인 제거 후 AC 조류분석 재실행 → 3가지 기준으로 취약점 판별
     - 취약점 기준: 선로 과부하(flow > rate), 전압 위반(V < 0.9 or V > 1.1), 섬격리(islanding)
   - `tda/vulnerability.py`: compare_with_homology(), compare_metrics_vulnerability()
     - alignment score = |intersection| / total_edges
     - precision, recall, specificity 포함
   - `tests/test_contingency.py`: AC 조류분석 + N-1 + 사이클 추출 + 정렬 점수 통합 테스트 23개
   - 총 82개 테스트 모두 통과

✅ 해결됨 (2026-07-12 14:15): **P1 — LODFInverseDistance + KCLCurrentDistance GUI 통합**
   - LODFInverseDistance 메트릭 구현 (`electrical_distance/metrics.py`)
   - KCLCurrentDistance GUI 통합 (`integration/power_grid_tda.py`)
   - compare_metrics_vulnerability() metric_fns에 추가
   - 단위 테스트 6개 추가 (총 53→59)
   - 자세한 내용은 history.md 2026-07-12 14:15 참조

✅ 해결됨 (2026-07-11 22:30): **Bus LODF Sensitivity NaN 전파 버그 수정**
   - 원인: `compute_lodf()`에서 bridge/leaf 라인 (분모=0)에 대해 `np.nan`을 설정하여 전체 거리 행렬이 NaN이 됨
   - 수정: bridge 라인 LODF를 `np.nan` 대신 `0.0`으로 설정 (표준 근사법)
   - 영향: 우크라이나 18-bus, 28-bus 그리드 모두 정상 작동 확인
   - 신규 테스트 2개 추가 (53개 모두 통과)

✅ 해결됨 (2026-07-11): _vulnerability_analysis 및 관련 메서드들이 _on_undo 내부에 로컬 함수로 잘못 정의되어 있었음. GraphEditor 클래스의 실제 메서드로 승격 완료.

✅ 해결됨 (2026-07-11): VR Complex H1 사이클 death가 infinity로 잘못 표시되던 버그 수정. persistence_pairs()의 triangle killing 루프에서 break 제거 및 youngest edge rule 개선.

✅ 해결됨 (2026-07-11): Bus LODF Sensitivity 메트릭의 대칭성 문제 수정. 기존 abs-sum 방식은 |LODF| 합계를 사용하여 대칭적 그리드에서 버스들이 동일한 민감도 벡터를 갖는 문제 발생. 새로운 PTDF-weighted signed LODF 방식 (v_i[k] = Σ PTDF[l,i] × LODF[l,k])으로 대체하여 모든 버스 쌍에서 고유한 거리 보장. 3-bus 및 5-bus 테스트 통과.

✅ 해결됨 (2026-07-11 18:00): `graph_editor.py` Ukraine 섹션 들여쓰기 오류 수정. 16-space → 8-space로 변경.

✅ 해결됨 (2026-07-11 19:30): 로그 시스템 구축 완료 (`utils/logger.py`, `logs/` 디렉토리). RotatingFileHandler 기반.

✅ 해결됨 (2026-07-11 19:30): 취약점 분석 PowerGridTDAExplorer에 통합 완료. ⚠ 버튼 + 결과 창 + VR 색상 표시.

✅ 해결됨 (2026-07-11 20:30): `integration/power_grid_tda.py` IndentationError 수정 (버튼 섹션 16-space → 8-space). 취약점 분석 3개 메서드 (`_vulnerability_analysis`, `_show_vulnerability_window`, `_color_vr_nodes_by_vulnerability`) 전체 파일에 재작성하여 통합 완료.

✅ 해결됨 (2026-07-11): persistence diagram에서 birth=death 점 필터링. `graph_editor.py`의 `_draw_pd()`, `power_grid_tda.py`의 `_draw_persistence_diagram()`, matplotlib scatter section 총 3곳에서 `b >= d - 1e-12` 조건으로 대각선 위 점들을 제거하여 시각적 노이즈 제거.

✅ 해결됨 (2026-07-11 21:00): **TDA 탐색기 vs PowerGridTDAExplorer VR 결과 불일치 문제 해결**
   - 원인: `graph_editor.py`의 `_tda_explorer()`가 `tda/vr_core.py`의 `VRComplex` 클래스를 사용하지 않고 **자체 inline VR 구현**을 사용하고 있었음
   - inline 구현의 H1 triangle killing 로직이 `break` 문으로 인해 첫 번째 발견된 삼각형만 처리하는 버그 존재
   - `betti_numbers()`도 inline union-find로 중복 구현되어 VRComplex와 다른 결과 생성 가능
   - 수정: `_tda_explorer()`에서 inline VR 구현(약 80줄)을 제거하고 `VRComplex(D)` 클래스로 완전 대체
   - 슬라이더 update 함수의 Betti number 계산도 `vr.betti_numbers(alpha)`로 통일
   - 이제 동일한 거리 행렬에 대해 `_tda_explorer()`와 `PowerGridTDAExplorer`가 **완전히 동일한 persistence 결과**를 반환함

✅ 해결됨 (2026-07-12 14:30): **`integration/power_grid_tda.py` 삭제 복원**
   - git checkout으로 복구 완료
   - `.bak` 파일 정리 완료
   - 모든 파이썬 파일 파싱 정상, 53개 테스트 모두 통과 확인

---

### 미해결 이슈 (2026-07-12)

#### P2: 테스트 및 검증
- [ ] **실험 자동화 CLI 도구 부재**
  - 우크라이나 그리드 로드 → 모든 메트릭 → TDA → 취약점 비교 → 리포트 생성 파이프라인 없음
  - compare_metrics_vulnerability() 함수 존재하나 호출/표시 도구 없음

#### P2: GUI 개선
- [ ] **compare_metrics_vulnerability GUI 미통합**
  - 함수는 구현되었으나 GUI에서 호출/표시되지 않음
  - 메트릭별 alignment score 시각화 필요

#### P3: 문서화
- [ ] **README.md 구버전 정보**
  - "47 unit tests" → 실제 82개로 업데이트 필요
  - vulnerability.py API 문서화 안 됨
  - compare_metrics_vulnerability, KCLCurrentDistance 문서화 안 됨
  - N-1 contingency analysis 기준 문서화 안 됨
  - AC power flow solver 문서화 안 됨
