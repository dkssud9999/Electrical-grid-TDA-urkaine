# Core dependencies
numpy>=1.24   (request completed)

# Testing
pytest>=7.0  (request completed)

# Optional: for image capture in AI analysis
pillow  (request complted)

# Optional: for metric comparison charts
matplotlib  (request completed)

# Optional: for PyPSA format import
pypsa  (sudo pacman -S python-pypsa didnt worked, so i dont know, please recheck)

# Optional: for advanced matrix operations
scipy  (request completed)

